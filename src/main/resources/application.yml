spring:
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev

  doc:
    api-docs:
      path: /api-docs
    swagger-ui:
      path: /swagger-ui.html


app:
  name: parking-hexagonal-java

server:
  servlet:
    context-path: /api/v1


