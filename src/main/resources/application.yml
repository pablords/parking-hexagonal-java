spring:
  main:
    allow-bean-definition-overriding: true
  profiles:
    active: dev
  doc:
    api-docs:
      path: /api-docs
    swagger-ui:
      path: /swagger-ui.html

app:
  name: parking

server:
  servlet:
    context-path: /api/v1

logging:
  level:
    root: WARN
    com.pablords.parking: TRACE
